import 'package:flutter/material.dart';

class AddServiceBody extends StatelessWidget {
  const AddServiceBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add a Service'),
    );
  }
}
